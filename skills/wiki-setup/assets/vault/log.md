# Log

Append-only record of what happened to this wiki and when. Newest at the bottom. Every entry starts with `## [YYYY-MM-DD] <operation> | <subject>` so the file stays greppable.

Operations: setup, capture, ingest, query, lint, maintain, dream, schema.

## [{{YYYY-MM-DD}}] setup | vault created
- structure created, schema written for {{domain}}
- next: first source
